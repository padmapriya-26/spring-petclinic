ipeline {
    agent {
        label 'headche-label'
    }
    tools {
        maven 'Maven-3.8.8'
    }
    stages {
        stage('Build') {
            steps {
                echo "this is my spring pipeline"
                sh "mvn --version"
            }
        }
    }
}
