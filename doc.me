# Write a pipeline that pushes the Java artifact to a bucket and builds a Docker image using the artifact retrieved from the bucket.

first i need to create the 2 vms which are one with slave1 and slave2 in which one slave1 i need to do master slave connection and also need to install the java and maven
and another i need to do the install java,docker 
in slave2 also give the permissions like sudo usermod -aG docker sony2
